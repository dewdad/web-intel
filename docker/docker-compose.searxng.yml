services:
  searxng:
    image: searxng/searxng:latest
    container_name: wrs-searxng
    ports:
      - "${SEARXNG_HOST_PORT:-9871}:8080"
    volumes:
      - ./searxng:/etc/searxng:rw
    environment:
      - SEARXNG_BASE_URL=http://localhost:${SEARXNG_HOST_PORT:-9871}/
    depends_on:
      - valkey
    restart: unless-stopped

  valkey:
    image: valkey/valkey:8-alpine
    container_name: wrs-valkey
    command: valkey-server --save 30 1 --loglevel warning
    volumes:
      - valkey-data:/data
    restart: unless-stopped

volumes:
  valkey-data:
